---
import { themeDiagram } from '../lib/diagram-theme';
import { renderDot } from '../lib/dot';
interface Props {
  source: string;
  alt: string;
}
const { source, alt } = Astro.props;
const svg = await renderDot(source);
---

<div
  class="diagram-figure"
  role="img"
  aria-label={alt}
  set:html={themeDiagram(svg)}
/>
