---
interface Props {
  active: 'leic' | 'meic';
}
const { active } = Astro.props;
---

<nav class="program-nav" aria-label="Curso">
  <a href="/#cadeiras" aria-current={active === 'leic' ? 'page' : undefined}>
    LEIC
  </a>
  <a href="/meic/" aria-current={active === 'meic' ? 'page' : undefined}>
    MEIC
  </a>
</nav>
