---
title: Geração de código e registos
description: Seleção de instruções, alocação de registos por coloração e Jasmin para uma expressão com a pilha de operandos.
section: conteudo
order: 7
---

import Dot from '../../../components/Dot.astro';
import CodePlayground from '../../../components/CodePlayground.astro';
import interferencia from '../../c/interferencia.dot?raw';

A geração de código traduz o código intermédio para as instruções da máquina alvo. São três decisões encadeadas: **seleção** (que instruções cobrem cada operação), **escalonamento** (por que ordem, respeitando dependências) e **alocação de registos** (que valores vivem em registos e quais ficam em memória).

## Escalonar respeitando dependências

O escalonador pode reordenar instruções para esconder latências, mas nunca pode trocar a ordem de duas instruções com dependência leitura após escrita (RAW): a leitura tem de ver o valor já escrito. Toma este bloco:

```
t1 = a + b
t2 = t1 * c
t3 = a + b
```

`t2` lê `t1`, por isso `t2` vem sempre depois de `t1`. Mas `t3` só lê `a` e `b`, que mais ninguém escreve aqui: é independente das duas. Ordens válidas: `t1, t2, t3`, `t1, t3, t2` e `t3, t1, t2`. A ordem `t1, t3, t2` é a mais esperta se a soma demorar: enquanto `t1` fica pronto, o processador adianta `t3` em vez de esperar parado pelo `t2`. Em teste, sublinha primeiro as dependências e só depois propõe a ordem; uma troca que viole uma RAW está errada mesmo que pareça mais rápida.

## Alocação por coloração

Os registos são poucos e os temporários são muitos. O compilador constrói o **grafo de interferência**: um nó por valor vivo, uma aresta entre dois valores vivos ao mesmo tempo. Colorir o grafo com $k$ cores (os $k$ registos) é atribuir registos sem conflitos: vizinhos ficam em registos diferentes.

Quando $k$ cores não chegam, algum valor é **derramado** (_spill_): em vez de registo, vive em memória, com um carregamento antes de cada uso e uma gravação depois de cada definição. O derrame resolve o conflito à custa de acessos extra, por isso o compilador escolhe a vítima pelo custo: derrama o valor usado menos vezes dentro de ciclos.

<Dot
  source={interferencia}
  alt="Grafo de interferência com quatro valores: a, b e c ligados entre si em triângulo, d ligado só a c, e c marcado com círculo duplo como vítima de derrame."
/>

Com $k = 2$ registos, o triângulo $a$, $b$, $c$ não tem coloração: $a$ e $b$ ocupam os dois registos e $c$ conflita com ambos. Se $c$ for o valor usado menos vezes (fora de ciclos), é a vítima: vive em memória com `istore` e `iload` à volta de cada uso, e $d$, que só conflitava com $c$, fica livre para um dos registos. A marca de círculo duplo é a decisão do alocador, não uma propriedade do programa.

## Exemplo: Jasmin para `(a + b) * c`

No projeto, o alvo é a JVM através do Jasmin, que usa uma **pilha de operandos**: as instruções tiram operandos da pilha e empurram o resultado. Com `a`, `b`, `c` nas variáveis locais 1, 2 e 3, e o resultado na 4:

```jasmin
iload_1
iload_2
iadd
iload_3
imul
istore 4
```

Segue a pilha: empurra `a`, empurra `b`, `iadd` consome os dois e empurra a soma; empurra `c`, `imul` consome soma e `c` e empurra o produto; `istore 4` guarda-o. A altura máxima é 2, e o verificador da JVM confirma-a estaticamente: cada instrução declara o seu efeito na pilha, por isso um desequilíbrio é erro de compilação, não surpresa em execução.[^pilha]

Confirma a altura máxima a correr. A simulação abaixo executa o mesmo exemplo instrução a instrução e imprime a pilha final com a altura máxima.

<CodePlayground
  language="python"
  title="Pilha de operandos"
  code={
    'pilha = []\nmaxima = 0\nfor passo in ["iload a", "iload b", "iadd", "iload c", "imul", "istore x"]:\n    if passo.startswith("iload"):\n        pilha.append(passo.split()[1])\n    elif passo in ("iadd", "imul"):\n        pilha.pop()\n        pilha.pop()\n        pilha.append("t")\n    else:\n        pilha.pop()\n    maxima = max(maxima, len(pilha))\nprint(pilha, maxima)'
  }
/>

O programa imprime `[] 2`: a pilha termina vazia (tudo consumido e guardado) e nunca passou de 2. Muda a expressão, por exemplo para `(a + b) * (c + d)`, e vê a altura subir para 3.

[^pilha]: O efeito de cada instrução Jasmin na pilha está na [referência oficial de instruções](https://jasmin.sourceforge.net/instructions.html). A verificação estática da altura é do verificador da JVM (especificação, capítulo 4.10).

Onde entra o derrame aqui? As variáveis locais do Jasmin funcionam como a memória de derrame: quando a expressão precisa de mais valores vivos do que os registos (ou as posições de pilha) disponíveis, o gerador guarda temporários em locais extra com `istore` e recarrega-os com `iload`, exatamente o par gravação-carregamento do derrame. Numa expressão maior, verias `istore` a meio do cálculo a guardar um parcial e `iload` a trazê-lo de volta quando preciso.

## Para saber mais

- [Manual do Jasmin](https://jasmin.sourceforge.net/guide.html): sintaxe de classes e métodos para escrever o alvo do projeto.
- [Tutorial de JVM e Jasmin](http://saksagan.ceng.metu.edu.tr/courses/ceng444/link/f3jasmintutorial.html): exemplos corridos de pilha e variáveis locais.

## Para levar para a próxima página

O código corre e está correto. Mas antes de o entregar, há transformações que o tornam mais rápido sem lhe mudar o significado: a [análise e otimização](otimizacao-codigo/).
