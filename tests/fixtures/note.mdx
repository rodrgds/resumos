import Typst from '../../src/components/Typst.astro';
import fragment from './fragment.typ?raw';
import diagram from './diagram.typ?raw';

# Exemplo MDX

LaTeX no Markdown: $a^2 + b^2 = c^2$.

<Typst source={fragment} />
<Typst
  source={diagram}
  format="svg"
  alt="Uma entrada transforma-se numa saída."
/>
